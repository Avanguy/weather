import React from 'react'

const CWeather = ({WInfo}) => {
    console.log(WInfo);
    return (
        <div className = "cWInfo">
            test
        </div>
    )
}

export default CWeather
